services:
  whoami:
    image: traefik/whoami
    networks:
      - reverse-proxy_caddy
    labels:
      caddy: losbubos.duckdns.org
      caddy.reverse_proxy: "{{upstreams 80}}"

networks:
  reverse-proxy_caddy:
    external: true