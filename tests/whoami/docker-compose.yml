services:
  whoami:
    image: traefik/whoami
    networks:
      - caddy
    labels:
      caddy: losbubos.duckdns.org
      caddy.reverse_proxy: "{{upstreams 80}}"

networks:
  caddy:
    external: true